{% extends './base.html.twig' %}

{% block title %} SearchMail{% endblock %}

{% block body %}

    <div class="container">
        <h1> Welcome to the search Mail</h1>

        <div class="row">
            {{ form_start(form) }}

            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success pull-right">Rechercher</button>
            {{ form_end(form) }}
        </div>

    </div>

{% endblock %}
